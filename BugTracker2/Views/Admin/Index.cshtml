@model IEnumerable<BugTracker2.Models.ApplicationUser>

@*<header>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <img class="img-responsive" src="~/Template/img/profile.png" alt="">
                <div class="intro-text">
                    <span class="name">Bug Tracker</span>
                    <hr class="star-light">
                    <span class="skills">Project developed by Mary Ann Burns for Coder Foundry 2016</span>
                </div>
            </div>
        </div>
    </div>
</header>*@




<h2>User List</h2>

  

@foreach (var item in Model)
{

    <p>
        @Html.DisplayFor(modelItem => item.DisplayName)
        @Html.ActionLink("Assign Roles", "EditUser", "Admin", new { id = item.Id }, null) |
        @Html.ActionLink("Assign Projects", "ProjectUser", "Admin", new { id = item.Id }, null)
    </p>

    <p>
        @Html.ActionLink("View All Projects", "Index", "Projects", new { id = item.Id }, null)  |
        @Html.ActionLink("Tickets", "Index", "Tickets", new { id = item.Id }, null)
    </p>

}
